package com.jkanimetv.app.ui.screens

// Removed — video URL is now resolved directly via JKAnimeRepository.getVideoUrl()
// No server picker is shown; the original app also uses a single jkplayer source.
